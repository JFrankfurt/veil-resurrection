{
  "name": "@predictions/subgraph",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy predictions-v2",
    "create-local": "graph create --node http://localhost:8020/ predictions-v2",
    "remove-local": "graph remove --node http://localhost:8020/ predictions-v2",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 predictions-v2"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.68.0",
    "@graphprotocol/graph-ts": "^0.32.0"
  }
}
